# Starting the Project

```bash
# Install pnpm.
npm install pnpm -g

# (Optional) Set the image source.
pnpm config set registry https://registry.npmmirror.com

# Install dependencies.
pnpm install

# Start and run.
pnpm run dev
```
